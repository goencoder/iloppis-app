# Copy this file to keystore.properties and replace every placeholder.
# keystore.properties is ignored by Git and must never be committed.
storeFile=/absolute/path/to/iloppis-upload.jks
storePassword=REPLACE_WITH_KEYSTORE_PASSWORD
keyAlias=iloppis-upload
keyPassword=REPLACE_WITH_KEY_PASSWORD
